{
  "name": "massage-reserve-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "lint": "eslint --ext .js,.ts .",
    "verify-all-emails": "node ./scripts/verify-all-emails.mjs"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "luxon": "^3.5.0",
    "resend": "^4.8.0"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.1",
    "@types/node": "^22.15.0",
    "typescript": "^5.7.2"
  }
}
