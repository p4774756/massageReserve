---
description: 為本專案產 UI／素材圖時須透明背景、禁止白底外框
alwaysApply: true
---

# 產圖規範（透明背景）

適用於為 **massage-reserve** 產生或請模型繪製的任何 **PNG／WebP UI 素材**（按鈕、裝飾框、圖示擴充等），包含 Cursor **Generate image** 或其它影像生成流程。

## 強制要求

1. **透明背景**  
   - 圖檔必須帶 **alpha 通道**：按鈕／花框／物件**輪廓以外**的像素須為 **完全透明**。  
   - **禁止**整張圖底下墊 **白色（或任何不透明）矩形／畫布底色**，以免疊在深色 UI 上出現醜的白塊。

2. **禁止「假透明」**  
   - 不得以近似頁面底色取代透明（例如假裝融進背景的實色塊）；若要去背，就以 **真透明** 輸出。

3. **檢查方式（Agent／人類）**  
   - 匯入 `public/media/` 或元件背景前，在檢視器或設計軟體中確認 **棋盤格／透明區** 僅出現在物件外側，物件周圍無白邊或灰邊。

4. **提示詞習慣**  
   - 產圖時一律明確寫入：**transparent background, PNG alpha, no white matte, no rectangular canvas fill outside the subject**（可依語境改寫為中文對應說明）。

## 拉霸「收下」按鈕

- 資產路徑：**`public/media/slot-take-button-luxe.png`**  
- 須符合上述透明規範；按鈕本體由 CSS 放大顯示時，外側不得因白底而與深藍場景衝突。
