{
  "name": "massage-reserve",
  "private": true,
  "version": "1.8.1",
  "type": "module",
  "scripts": {
    "bump:patch": "npm version patch --no-git-tag-version",
    "bump:minor": "npm version minor --no-git-tag-version",
    "bump:major": "npm version major --no-git-tag-version",
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:full": "node --env-file=.env.e2e ./node_modules/@playwright/test/cli.js test"
  },
  "dependencies": {
    "echarts": "^5.6.0",
    "echarts-gl": "^2.0.9",
    "firebase": "^11.6.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
